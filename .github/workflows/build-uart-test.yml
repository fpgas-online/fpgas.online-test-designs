# .github/workflows/build-uart-test.yml
name: "Build: UART Test"

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build-arty:
    name: "Arty A7-35T"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install openXC7 toolchain and cross compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y yosys gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf
          wget -q https://github.com/openXC7/openXC7-snap/releases/download/0.8.2/openxc7_0.8.2_amd64.snap
          sudo snap install --classic --dangerous ./openxc7_0.8.2_amd64.snap
          sudo snap alias openxc7.nextpnr-xilinx nextpnr-xilinx
          sudo snap alias openxc7.fasm2frames fasm2frames
          sudo snap alias openxc7.xc7frames2bit xc7frames2bit
          sudo snap alias openxc7.bbasm bbasm

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Python dependencies
        run: uv sync --extra build

      - name: Build Arty UART test bitstream
        env:
          CHIPDB: ${{ github.workspace }}/chipdb
        run: |
          mkdir -p "$CHIPDB"
          uv run python designs/uart/gateware/uart_soc_arty.py \
            --toolchain openxc7 \
            --build

      - name: Upload bitstream
        uses: actions/upload-artifact@v4
        with:
          name: uart-test-arty
          path: |
            designs/uart/build/arty/gateware/*.bit
          if-no-files-found: error

  build-netv2-a7-35:
    name: "NeTV2 A7-35T"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install openXC7 toolchain and cross compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y yosys gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf
          wget -q https://github.com/openXC7/openXC7-snap/releases/download/0.8.2/openxc7_0.8.2_amd64.snap
          sudo snap install --classic --dangerous ./openxc7_0.8.2_amd64.snap
          sudo snap alias openxc7.nextpnr-xilinx nextpnr-xilinx
          sudo snap alias openxc7.fasm2frames fasm2frames
          sudo snap alias openxc7.xc7frames2bit xc7frames2bit
          sudo snap alias openxc7.bbasm bbasm

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Python dependencies
        run: uv sync --extra build

      - name: Build NeTV2 A7-35T UART test bitstream
        env:
          CHIPDB: ${{ github.workspace }}/chipdb
        run: |
          mkdir -p "$CHIPDB"
          uv run python designs/uart/gateware/uart_soc_netv2.py \
            --variant a7-35 \
            --toolchain openxc7 \
            --build

      - name: Upload bitstream
        uses: actions/upload-artifact@v4
        with:
          name: uart-test-netv2-a7-35t
          path: |
            designs/uart/build/netv2/gateware/*.bit
          if-no-files-found: error

  build-netv2-a7-100:
    name: "NeTV2 A7-100T"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install openXC7 toolchain and cross compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y yosys gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf
          wget -q https://github.com/openXC7/openXC7-snap/releases/download/0.8.2/openxc7_0.8.2_amd64.snap
          sudo snap install --classic --dangerous ./openxc7_0.8.2_amd64.snap
          sudo snap alias openxc7.nextpnr-xilinx nextpnr-xilinx
          sudo snap alias openxc7.fasm2frames fasm2frames
          sudo snap alias openxc7.xc7frames2bit xc7frames2bit
          sudo snap alias openxc7.bbasm bbasm

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Python dependencies
        run: uv sync --extra build

      - name: Build NeTV2 A7-100T UART test bitstream
        env:
          CHIPDB: ${{ github.workspace }}/chipdb
        run: |
          mkdir -p "$CHIPDB"
          uv run python designs/uart/gateware/uart_soc_netv2.py \
            --variant a7-100 \
            --toolchain openxc7 \
            --build

      - name: Upload bitstream
        uses: actions/upload-artifact@v4
        with:
          name: uart-test-netv2-a7-100t
          path: |
            designs/uart/build/netv2/gateware/*.bit
          if-no-files-found: error

  build-fomu:
    name: "Fomu EVT"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install iCE40 toolchain and cross compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y yosys nextpnr-ice40 fpga-icestorm gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Python dependencies
        run: uv sync --extra build

      - name: Build Fomu UART test bitstream
        run: |
          uv run python designs/uart/gateware/uart_soc_fomu.py \
            --build

      - name: Upload bitstream
        uses: actions/upload-artifact@v4
        with:
          name: uart-test-fomu
          path: |
            designs/uart/build/fomu/gateware/*.bin
          if-no-files-found: error

  build-tt:
    name: "TT FPGA Demo Board"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install iCE40 toolchain and cross compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y yosys nextpnr-ice40 fpga-icestorm gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Python dependencies
        run: uv sync --extra build

      - name: Build TT FPGA UART test bitstream
        run: |
          uv run python designs/uart/gateware/uart_soc_tt.py \
            --build

      - name: Upload bitstream
        uses: actions/upload-artifact@v4
        with:
          name: uart-test-tt-fpga
          path: |
            designs/uart/build/tt/gateware/*.bin
          if-no-files-found: error

  lint:
    name: "Lint Python"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Lint
        run: uv run --extra dev ruff check designs/uart/gateware/

  build-acorn-cle-215plus:
    name: "Acorn CLE-215+"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install openXC7 toolchain and cross compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y yosys gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf
          wget -q https://github.com/openXC7/openXC7-snap/releases/download/0.8.2/openxc7_0.8.2_amd64.snap
          sudo snap install --classic --dangerous ./openxc7_0.8.2_amd64.snap
          sudo snap alias openxc7.nextpnr-xilinx nextpnr-xilinx
          sudo snap alias openxc7.fasm2frames fasm2frames
          sudo snap alias openxc7.xc7frames2bit xc7frames2bit
          sudo snap alias openxc7.bbasm bbasm

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Python dependencies
        run: uv sync --extra build

      - name: Build Acorn CLE-215+ bitstream
        env:
          CHIPDB: ${{ github.workspace }}/chipdb
        run: |
          mkdir -p "$CHIPDB"
          uv run python designs/uart/gateware/uart_soc_acorn.py \
            --variant cle-215+ \
            --toolchain openxc7 \
            --build

      - name: Upload bitstream
        uses: actions/upload-artifact@v4
        with:
          name: uart-test-acorn-cle-215plus
          path: |
            designs/uart/build/acorn/gateware/*.bit
          if-no-files-found: error

  build-acorn-cle-215:
    name: "NiteFury (CLE-215)"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install openXC7 toolchain and cross compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y yosys gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf
          wget -q https://github.com/openXC7/openXC7-snap/releases/download/0.8.2/openxc7_0.8.2_amd64.snap
          sudo snap install --classic --dangerous ./openxc7_0.8.2_amd64.snap
          sudo snap alias openxc7.nextpnr-xilinx nextpnr-xilinx
          sudo snap alias openxc7.fasm2frames fasm2frames
          sudo snap alias openxc7.xc7frames2bit xc7frames2bit
          sudo snap alias openxc7.bbasm bbasm

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Python dependencies
        run: uv sync --extra build

      - name: Build NiteFury (CLE-215) bitstream
        env:
          CHIPDB: ${{ github.workspace }}/chipdb
        run: |
          mkdir -p "$CHIPDB"
          uv run python designs/uart/gateware/uart_soc_acorn.py \
            --variant cle-215 \
            --toolchain openxc7 \
            --build

      - name: Upload bitstream
        uses: actions/upload-artifact@v4
        with:
          name: uart-test-acorn-cle-215
          path: |
            designs/uart/build/acorn/gateware/*.bit
          if-no-files-found: error

  build-acorn-cle-101:
    name: "LiteFury (CLE-101)"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install openXC7 toolchain and cross compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y yosys gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf
          wget -q https://github.com/openXC7/openXC7-snap/releases/download/0.8.2/openxc7_0.8.2_amd64.snap
          sudo snap install --classic --dangerous ./openxc7_0.8.2_amd64.snap
          sudo snap alias openxc7.nextpnr-xilinx nextpnr-xilinx
          sudo snap alias openxc7.fasm2frames fasm2frames
          sudo snap alias openxc7.xc7frames2bit xc7frames2bit
          sudo snap alias openxc7.bbasm bbasm

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Python dependencies
        run: uv sync --extra build

      - name: Build LiteFury (CLE-101) bitstream
        env:
          CHIPDB: ${{ github.workspace }}/chipdb
        run: |
          mkdir -p "$CHIPDB"
          uv run python designs/uart/gateware/uart_soc_acorn.py \
            --variant cle-101 \
            --toolchain openxc7 \
            --build

      - name: Build PLL diagnostic bitstream
        env:
          CHIPDB: ${{ github.workspace }}/chipdb
        run: |
          mkdir -p "$CHIPDB"
          uv run python tmp/diag_pll.py \
            --variant cle-101 \
            --toolchain openxc7 \
            --build

      - name: Build BRAM init diagnostic bitstream
        env:
          CHIPDB: ${{ github.workspace }}/chipdb
        run: |
          mkdir -p "$CHIPDB"
          uv run python tmp/diag_bram.py \
            --variant cle-101 \
            --toolchain openxc7 \
            --build

      - name: Upload bitstream and build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: uart-test-acorn-cle-101
          path: |
            designs/uart/build/acorn/gateware/*.bit
            designs/uart/build/acorn/gateware/*.fasm
            designs/uart/build/acorn/gateware/*.init
            designs/uart/build/acorn/software/bios/bios.bin
            tmp/build/diag_pll/*.bit
            tmp/build/diag_bram/*.bit
          if-no-files-found: error
